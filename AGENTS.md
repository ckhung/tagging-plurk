# AGENTS.md

### 一、 任務概述

本目錄之任務為處理噗浪 (Plurk) 短文的標籤分類系統， 包含檢查標籤適用性或對噗文提出增/刪/修改的建議等等。 本 Agent 僅負責讀取資料庫並生成變更建議報告，並不直接修改資料庫，其任務具備冪等性 (Idempotency)。 

### 二、 可用查詢工具與資料架構

#### 1. 知識庫與資料模型

* **標籤階層定義**： 見本文最後一節。 該結構在數學上為一個**偏序集 (Poset)**，並以**有向無環圖 (DAG)** 形式呈現。部分標籤（如「監控」、「認知戰」）具備多重父節點（多重繼承）。
* **資料庫結構描述 (Schema)**：`plurk_tags.db` (SQLite3 格式)，包含以下兩個資料表：

```sql
CREATE TABLE "plurk" (
    id TEXT PRIMARY KEY,    -- 噗浪官方 ID ( e.g., '3it94d1ivx' )
    ts TEXT NOT NULL,       -- 時間戳記字串 ( e.g., '260619-0218' )
    content TEXT NOT NULL   -- 含有 <li> 的原始 HTML 內容
);
CREATE TABLE "plurk_tag" (
    plurk_id TEXT NOT NULL,
    tag TEXT NOT NULL,
    PRIMARY KEY (plurk_id, tag),
    FOREIGN KEY (plurk_id) REFERENCES plurk(id) ON DELETE CASCADE
);
```

#### 2. 查詢工具與命令範例

你可以透過本地腳本 `Q.php` 或直接使用 `sqlite3` 進行資料檢索：

* **查詢特定標籤噗文聯集**：
`php Q.php 'T=科學+動物+微軟' id=yes`
這會同時取得每一噗之既有標籤， 非常簡單方便。
* **查詢所有標籤之噗文數量統計**：
`php Q.php`
* **進階 SQL 查詢**：
`sqlite3 plurk_tags.db "SELECT COUNT(*) FROM plurk;"`
或其他更複雜的 sqlite3 指令查詢。

註：`Q.php` 輸出格式為 HTML `<li>` 列表， 每筆資料一列。 為降低 Token 消耗與閱讀負擔，建議透過 pipe 搭配 Perl 進行正則表達式過濾：
```bash
php Q.php 'T=超限戰' id=yes | perl -ne 'next unless /<li>/; s#</?\w[^>]*>##g; print'
```

過濾後的輸出每列首位均為噗浪官方指定的 id， 即 plurk table 的主鍵， 其次為 6 碼之 `YYMMDD-HHMM` 時間戳記， 可供 grep 篩選日期。

### 三、 通用步驟規則

本文後面敘述的幾種不同任務可能會需要用到某些相同的步驟或判斷。 本節規範執行這些步驟或判斷時的注意事項。
1. 優先採用「階段隔離 (stage isolation)」， 或者說 loop fission 的方式執行任務， 以避免頻繁 context switch。
    要這樣做：
```
    foreach 噗：
        產生概念摘錄
    foreach 噗：
        挑選一個適用其概念摘錄的標籤
    foreach 噗：
        ... 更多步驟
```
    不要這樣做：
```
    foreach 噗：
        產生概念摘錄
        挑選一個適用其概念摘錄的標籤
        ... 更多步驟
```
2. **概念摘錄 (concept extraction)**： 為某一噗產生概念摘錄時， 以大約 60 個以內的 tokens 為限。 若噗文無可供判讀之實質文本 (例如僅含圖片) 則以 ∅  作為其概念摘錄。
3. **寬鬆解釋標籤**： 因為 「標籤階層定義」 非常小， 子標籤的 siblings 也很少， 甚至沒有， 所以應盡量採用最廣義、 最寬鬆的方式解讀， 最大化每個標籤的涵蓋範圍， 除非與 siblings 重疊。
4. **無標籤狀況**： 若某噗的概念摘錄為 ∅ ， 或無任何合適的標籤， 則標示固定標籤 `＊無標籤`。 (為便利用戶分辨未處理 vs 已處理但無標籤的噗文。)
5. **遞移約簡 (Transitive Reduction) 原則**：若某一噗文之語義同時符合子節點標籤與父（或祖先）節點標籤，**務必僅保留最深層之子節點標籤**，移除所有上層祖先標籤，以維持資訊密度的極大化。
6. **時間戳記邊界**：指定日期範圍時，應採取**閉區間** 處理，包含起迄日當天 00:00 至 24:00 之所有資料。
7. 將概念摘錄轉成文字輸出時， 可用中文或英文， 亦可兩者夾雜。
8. 產出供人工檢視的純文字檔時， 檔名採此格式： rep-mmddHHMM.txt 其中 mmddHHMM 為產生時刻的 「月日時分」。
9. **分批處理**： 不同任務類型應採用不同批次大小， 以降低推理負擔、 確保標籤檢查或標示的品質。 如果上限是 20 則， 而輸入資料是 21 則， 則採平均分配原則分為 11+10 兩批處理。

### 三、 新增型任務

使用者指定一個範圍的噗文， 要求進行分類。
1. 讀取指定範圍的所有噗文， **忽略所有現有標籤**。
2. 對每一噗產生概念摘錄。
3. 對每一噗的概念摘錄， 從 「標籤階層定義」 內， 挑選一至數個適用的標籤。
4. 產出供人工檢視的純文字檔， 每噗一列， 格式為：`[id] [標籤1] [標籤2] ... %% [概念摘錄]`
每個批次最多 20 則。

### 四、 全面檢查型任務

使用者指定一個範圍的噗文， 要求檢查既有標籤是否適用。
1. 讀取指定範圍的所有噗文， **及每一則的現有標籤**。
2. 對每一噗產生概念摘錄。
3. 對每一噗的概念摘錄， 從 「標籤階層定義」 內， 挑選一至數個適用的標籤。
4. 對每一噗， 比對你所挑選的標籤與現有標籤是否一致。
5. 產出供人工檢視的純文字檔， 每噗一列， 格式為：`[id] [+標籤1] [+標籤2] [-標籤3] %% [概念摘錄]`
   報告時請忽略不需要更動的噗， 僅針對需要修正的噗文進行回報。
每個批次最多 10 則。

### 五、 聚焦檢查型任務

使用者指定一到兩個標籤， 其中有可能包含「標籤階層定義」當中某個現有標籤的替換版， 並要求你對每一則噗文檢查這少數幾個標籤是否適用? 是否需要更動資料庫?
1. 讀取指定範圍的所有噗文， **及每一則的現有標籤**。
2. 對每一噗產生概念摘錄， 特別要提及與指定標籤相關的概念； **暫時忽略此噗其他所有標籤**。
3. 對每一噗的概念摘錄， 請 **以寬鬆的標準** 檢視標示這幾個標籤是否恰當。 必要時可建議改採其 ancestor 或改採更精確的 descendant。
4. 拿上一步的成果與既有標籤比對， 產出供人工檢視的純文字檔， 每噗一列， 格式為： `[id] [+標籤1] [+標籤2] [-標籤3] %% [概念摘錄]`
   忽略不需要更動的噗， 僅針對需要修正的噗文進行回報。
每個批次最多 50 則。

### 六、 新標籤提案

使用者要求你閱讀大量噗文， 找出可能適用於三則以上噗文的一或多個新標籤。
1. 讀取指定範圍的所有噗文， **忽略所有現有標籤**。
2. 對每一噗產生概念摘錄。
3. 對所有噗的概念摘錄進行聚類 (clustering)。
4. 檢視有沒有哪些 cluster 含有至少三則噗文且適合用某個標籤來標示。
5. 如果有， 則與 「標籤階層定義」 進行比對。
   1. 針對每一個可能的新標籤提案， 在 「標籤階層定義」 內尋找適當的位置。 如果它已在定義中， 則忽略此標籤。
   2. 針對每一個確有新意的標籤提案， 寫出標籤名稱及簡單解釋， 隨後跟著適用此標籤的噗文， 每噗一列， 格式為： `[id] %% [概念摘錄]`。
如果沒有留下確有新意的標籤提案， 就回答 「找不到」。
概念摘錄階段每個批次最多 50 則； 後續則不分批。

### 七、 行為準則與限制

1. 除了工作需要的暫存檔以及產出供人工檢視的純文字檔之外， 嚴禁對檔案系統進行任何更動。
2. 再次強調： 避免頻繁 context switch， 務必盡力以階段隔離方式執行任務。

### 八、 標籤階層定義

註：本定義中，若不同路徑下出現完全相同之標籤名稱（例如『監控』、『網路管制』、『認知戰』... 等等），在數學上視為同一節點之多重繼承 (Multiple inheritance)，其祖先集合為所有出現路徑之上層節點聯集。

科學:
    數學:
    物理:
    航太:
    生醫:
    健康:
    資訊科技:
        資料視覺化:
        自由軟體:
            OSM:
        隱私:
            監控: # 具多重屬性 (Multiple inheritance)
        資安:
            詐騙:
                詐騙園區:
            網路管制: # 具多重屬性
                防火長城: # 具多重屬性
            海纜:
        雲端平台:
            google:
            微軟:
            line:
            meta:
        AI:
            prompting:
            深偽:
中共:
    中共軍事:
    中共內閧:
    中共國經濟:
    中共國日常:
        國有器官:
        香港:
        維吾爾族:
        維權:
    超限戰:
        認知戰: # 具多重屬性
            demoralization:
        對抗認知戰: # 具多重屬性
        網路管制: # 具多重屬性
            防火長城: # 具多重屬性
        監控: # 具多重屬性
    反共華人:
注意力經濟:
    媒體識讀:
        認知戰: # 具多重屬性
        對抗認知戰: # 具多重屬性
用戶權益:
    維修權:
    無良企業:
    良心企業:
好笑:
    parody:
    自我打臉:
哲理:
    寓言:
    預言:
    邏輯:
歷史:
國際:
政治:
法律:
性平:
語言:
民生:
心理:
環保:
學術研究:
教育:
文化:
武術:
當責:
動物:
